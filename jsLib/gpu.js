class GPU extends component {

    /**
     *
     * @param {string} address
     * @param {boolean} reset
     * @returns {*[]}
     */
    bind(address, reset) {
    }

    /**
     *
     * @param {number} xOffset
     * @param {number} yOffset
     * @param {string} char
     * @param {boolean} [vertical=false]
     * @returns {boolean}
     */
    set(xOffset, yOffset, char, vertical) {

    }

    constructor() {
    }
}